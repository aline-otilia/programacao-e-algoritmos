// 3) Elaborar um algoritmo que informe se um número informado pelo usuário é par ou ímpsr. 

var numero = parseInt(prompt("Digite um número: "));

if (numero % 2) {
    alert(" O número é ímpar ");
} else {
    alert("O número é par ");
}